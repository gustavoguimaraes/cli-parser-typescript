// Public entry point — consumers import from here.
// Internal types (ArgumentDefinition, Token, etc.) are intentionally omitted.
export { CommandBuilder } from "./command-builder.js";
export type { ArgumentOptions, ArgumentType, ParsedArguments } from "./types.js";
